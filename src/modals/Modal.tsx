// interface Props {
//   hideModal: () => void;
// }

//   ({
//   hideModal,
// }: Props)
function Modal() {
  return (
    // <div onClick={hideModal} className="modal-background">
    //   <div onClick={(e) => e.stopPropagation()} className="modal" />
    // </div>
    <h1>Arregla lo que esta comentado de aqui arriba</h1>
  );
}

export default Modal;
